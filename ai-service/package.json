{
  "name": "veridion-ai-service",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "dev": "node src/server.js"
  },
  "dependencies": {
    "amqplib": "^0.10.4",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "groq-sdk": "^1.5.0",
    "neo4j-driver": "^5.24.0",
    "pdf-parse": "^1.1.1",
    "pg": "^8.11.5",
    "tesseract.js": "^5.1.0"
  },
  "_systemRequirements": "poppler-utils must be installed on the host for scanned-PDF OCR (`apt-get install -y poppler-utils` on Debian/Ubuntu; already preinstalled on most CI/sandbox images). Used via pdftoppm/pdfinfo — no native Node addon required. See src/agents/ocr.js for why canvas/@napi-rs/canvas/pdfjs-dist were removed."
}
